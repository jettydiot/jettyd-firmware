if(CONFIG_SOC_WIFI_SUPPORTED)
    set(WIFI_COMPONENT_DEPS esp_wifi)
else()
    set(WIFI_COMPONENT_DEPS "")
endif()

idf_component_register(
    SRCS
        "src/jettyd.c"
        "src/mqtt.c"
        "src/ota.c"
        "src/provision.c"
        "src/shadow.c"
        "src/nvs.c"
        "src/wifi.c"
        "src/telemetry.c"
        "src/vm.c"
        "src/driver_registry.c"
        "src/manifest.c"
        "src/mcp.c"
    INCLUDE_DIRS
        "include"
    REQUIRES
        nvs_flash
        ${WIFI_COMPONENT_DEPS}
        esp_event
        esp_netif
        mqtt
        esp_https_ota
        esp_http_client
        json
        mbedtls
        esp_timer
        esp_system
        freertos
        driver
        app_update
        esp_partition
    PRIV_REQUIRES
        esp_driver_tsens
)
